sudo -i
yum install git -y

docker network create my-network
docker volume create mysql-data

docker run -d \
  --name mysql-container \
  --network my-network \
  -e MYSQL_ROOT_PASSWORD=Rajan@123 \
  -e MYSQL_DATABASE=employee_db \
  mysql:8


git clone https://github.com/Ramasamy3488/devops-springboot-employees-crud.git

cd devops-springboot-employees-crud

docker build -t springboot-employees-app:v1 .



docker run -d \
--name employeescon \
--network my-network \
-p 8080:8080 \
springboot-employees-app:v1


///////////////////////////////////////////////////

docker volume create mysql-data

docker run -d \
  --name mysql-container \
  --network my-network \
  --restart=unless-stopped \
  -e MYSQL_ROOT_PASSWORD=Rajan@123 \
  -e MYSQL_DATABASE=employee_db \
  -p 3306:3306 \
  -v mysql-data:/var/lib/mysql \
  mysql:8

----------

docker run -d   --name mysql-container --restart=unless-stopped  --network my-network -e MYSQL_ROOT_PASSWORD=Rajan@123 -e MYSQL_DATABASE=mydb -v mysql-data:/var/lib/mysql -p 8082:8080 mysql:8


1. List All Volumes
	docker volume ls

docker volume inspect mysql-data

2. Inspect a Specific Volume
	"Mountpoint": "/var/lib/docker/volumes/mysql-data/_data"

3. Check Volume Data (Real-Time Debugging)
	cd /var/lib/docker/volumes/mysql-data/_data
	ls


4. Check Which Container Uses the Volume
	docker inspect mysql-container

5. Check Volume Usage (Quick Way)
	docker ps
	docker inspect <container-name>
